# CSOPESY MO3 - Marquee Console settings. Read once when the program starts,
# from the working directory or the folder holding the executable. One
# "key value" pair per line; "#" starts a comment; "-" and "_" in a key are
# the same. A missing file, an unknown key or an unusable value keeps the
# built-in default, and the settings in force are printed under the welcome
# header. Editing this file never needs a rebuild: edit, save, run again.

marquee-text "Hello, World!"   # scrolling text; quotes optional, stripped
refresh-rate 100               # marquee frame interval in ms, 1 or more
polling-rate 10                # keyboard check interval in ms, 1 to 1000
